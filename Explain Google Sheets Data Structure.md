# Employee Database — AI Context Guide

## 1. Overview

هذا الملف يشرح قاعدة بيانات الموظفين الموجودة في ورقة:

- **Sheet Name:** `Emp_Data`
- **Main Table Name:** `Employee`
- **Header Range:** `A1:AH1`
- **Data Range:** `A2:AH56`
- **Primary Key:** `Code`
- **Last Reviewed:** 2026-07-27

كل صف يمثل موظفاً واحداً، باستثناء آخر صفّين اللذين يحتويان حالياً على الرقم التسلسلي وكود الموظف فقط.

قاعدة البيانات تحتوي على معلومات:

- تعريفية وشخصية.
- وظيفية وتنظيمية.
- معلومات المشاريع والمواقع.
- معلومات الاتصال.
- مستندات الهوية والإقامة.
- بيانات الحساب البنكي.
- تواريخ التعيين وانتهاء المستندات والعقود.
- التصنيف الإداري والفني للموظف.

---

## 2. Important Privacy Rules

البيانات تحتوي على معلومات شخصية شديدة الحساسية مثل:

- أسماء الموظفين.
- أرقام الجوازات والبطاقات المدنية.
- أرقام الهواتف.
- البريد الإلكتروني.
- أرقام الحسابات البنكية.
- تواريخ الميلاد.
- جهات الاتصال في حالات الطوارئ.

يجب على الذكاء الاصطناعي اتباع القواعد التالية:

1. عدم عرض أرقام الجوازات أو الحسابات البنكية أو البطاقات المدنية كاملة.
2. عدم نشر معلومات موظف محدد إلا عند طلب المستخدم ذلك بشكل صريح ومصرح.
3. عند إعداد تقارير عامة، يجب استخدام بيانات مجمعة حسب القسم أو المشروع أو الجنسية.
4. عدم استخدام معلومات الموظفين للتنبؤ بالصحة أو الدين أو التوجه السياسي أو أي صفات حساسة غير موجودة صراحة في البيانات.
5. إخفاء البيانات الشخصية في الأمثلة باستخدام قيم مثل `EMP-XXXX` أو `****1234`.
6. عدم اعتبار الخلايا الفارغة أو الرمز `-` بيانات مؤكدة.

---

## 3. Data Dictionary

| Column | Field | Description | Expected Type |
|---|---|---|---|
| A | `Sr` | رقم تسلسلي للسجل داخل الجدول. | Integer |
| B | `Code` | الكود الداخلي الفريد للموظف. يستخدم كمفتاح أساسي. | Text / Integer |
| C | `Name` | الاسم الكامل للموظف باللغة الإنجليزية. | Text |
| D | `Name Arabic` | الاسم الكامل للموظف باللغة العربية. | Text |
| E | `Title` | المسمى الوظيفي للموظف. | Category |
| F | `Department` | الإدارة أو القطاع الرئيسي التابع له الموظف. | Category |
| G | `Section` | القسم أو الوحدة التنظيمية داخل الإدارة. | Category |
| H | `Project` | المشروع أو موقع العمل الحالي، مثل `HQ` أو `Ibri`. | Category |
| I | `Gender` | جنس الموظف، مثل `Male` أو `Female`. | Category |
| J | `Birth date` | تاريخ ميلاد الموظف. | Date |
| K | `Age` | عمر الموظف محسوب تلقائياً من تاريخ الميلاد باستخدام تاريخ اليوم. | Formula / Decimal |
| L | `Hiring date` | تاريخ تعيين أو مباشرة الموظف. | Date |
| M | `Nationality` | جنسية الموظف. | Category |
| N | `Passport No.` | رقم جواز السفر أو المستند المشابه. | Sensitive Text |
| O | `Civil No.` | رقم البطاقة المدنية أو رقم الإقامة. | Sensitive Text |
| P | `Email` | البريد الإلكتروني للموظف. | Sensitive Text |
| Q | `Telephone` | رقم الهاتف الرئيسي للموظف. | Sensitive Text |
| R | `الباقه` | قيمة الباقة المسجلة للموظف. قد تكون باقة هاتف أو اتصال، ويجب تأكيد معناها من مسؤول البيانات. | Number |
| S | `Acc. NO.` | رقم الحساب البنكي للموظف. يجب تخزينه ومعالجته كنص للحفاظ على الأصفار في بدايته. | Highly Sensitive Text |
| T | `Civil ID/Resident ID Exp date` | تاريخ انتهاء البطاقة المدنية أو الإقامة. | Date |
| U | `Passport exp date` | تاريخ انتهاء جواز السفر. | Date |
| V | `Bank` | اسم أو كود البنك. | Category |
| W | `Swift` | كود SWIFT الخاص بالبنك. | Text |
| X | `Graduation Year` | سنة التخرج. الحقل غير مستخدم حالياً. | Year |
| Y | `Local/Expat` | تصنيف الموظف كمواطن أو وافد. الحقل غير مستخدم حالياً. | Category |
| Z | `Emergency Cont. No.` | رقم الاتصال الأول في حالات الطوارئ. | Sensitive Text |
| AA | `Relationship` | صلة قرابة جهة الاتصال الأولى بالموظف. | Category |
| AB | `Emergency Cont. No.2` | رقم الاتصال الثاني في حالات الطوارئ. | Sensitive Text |
| AC | `تاريخ انتهاء العقد` | تاريخ انتهاء عقد الموظف. | Date |
| AD | `Relationship2` | صلة قرابة جهة الاتصال الثانية بالموظف. | Category |
| AE | `Remarks` | ملاحظات أو قيم إضافية. معنى القيم الرقمية الموجودة يحتاج إلى تأكيد. | Text / Number |
| AF | `Acting As` | الشخص أو الدور الإداري الذي يعمل الموظف تحت إشرافه أو بالنيابة عنه. | Text |
| AG | `Direct/Indirect` | تصنيف الموظف كعمالة مباشرة أو غير مباشرة. | Category |
| AH | `Class` | التصنيف الوظيفي العام، مثل `Engineering` أو `Other`. | Category |

---

## 4. Current Dataset Summary

وفقاً للبيانات الحالية:

- يوجد 55 رقماً تسلسلياً وكود موظف.
- يوجد 53 سجلاً يحتوي على اسم ومعلومات وظيفية.
- آخر سجلين يحتويان على `Sr` و`Code` فقط ويجب عدم اعتبارهما موظفين مكتملين.
- المشاريع أو المواقع الرئيسية المسجلة تشمل:
  - `Ibri`
  - `AL Rawdah`
  - `HQ`
- الجنسيات الرئيسية المسجلة:
  - `Egyptian`
  - `Oman`
- التصنيفات الرئيسية في `Class`:
  - `Engineering`
  - `Other`
- حقل `Age` محسوب تلقائياً ولا يجب إدخاله يدوياً.

الأرقام السابقة تصف النسخة الحالية فقط وقد تتغير عند إضافة أو حذف موظفين.

---

## 5. Missing Values

قد تظهر البيانات المفقودة في أكثر من صورة:

- خلية فارغة.
- الرمز `-`.
- سجل يحتوي على الكود فقط.
- تاريخ مخزن كنص بدلاً من تاريخ Excel حقيقي.

عند تحليل البيانات:

- تعامل مع الخلايا الفارغة والرمز `-` كقيم مفقودة.
- لا تعامل القيمة الرقمية `0` كقيمة مفقودة بشكل تلقائي.
- لا تستبدل القيم المفقودة بقيم متوقعة دون موافقة المستخدم.
- لا تفترض أن الموظف مواطن أو وافد اعتماداً على الجنسية فقط.

---

## 6. Data Quality Notes

توجد بعض النقاط التي يجب مراعاتها عند قراءة البيانات:

1. بعض القيم تحتوي على مسافات إضافية، مثل `Male ` و`Projects `.
2. توجد اختلافات بسيطة في كتابة الإدارات والأقسام.
3. بعض التواريخ مخزنة كتواريخ Excel وبعضها كنص.
4. تنسيق التواريخ غير موحد بين `DD/MM/YYYY` و`M/D/YYYY`.
5. أرقام الهواتف قد تبدأ بـ `+968` أو تكون بدون رمز الدولة.
6. أرقام الجوازات والبطاقات والحسابات البنكية يجب التعامل معها كنص وليس كأرقام.
7. الحقول `Graduation Year` و`Local/Expat` وبيانات الطوارئ غير معبأة حالياً.
8. حقل `Remarks` يحتوي على بعض القيم الرقمية، لكن معناها غير موضح.
9. بعض الموظفين لا يوجد لهم بريد إلكتروني أو معلومات بنكية أو تواريخ انتهاء مستندات.
10. لا يجب تعديل قيمة `Age` يدوياً لأنها ناتجة عن معادلة.

---

## 7. Recommended Normalization

قبل استخدام البيانات في نظام أو قاعدة بيانات، يفضل:

- إزالة المسافات الزائدة من بداية ونهاية النصوص.
- توحيد قيم الجنس إلى:
  - `Male`
  - `Female`
- توحيد أسماء المشاريع والإدارات والأقسام.
- تحويل جميع التواريخ إلى تنسيق ISO:

  `YYYY-MM-DD`

- تحويل أرقام الهواتف إلى تنسيق موحد، مثل:

  `+968XXXXXXXX`

- الاحتفاظ بأكواد الموظفين وأرقام المستندات والحسابات البنكية كحقول نصية.
- استخدام قيمة `NULL` للبيانات غير المتوفرة بدلاً من `-`.
- إضافة تعريف واضح لحقل `Remarks`.
- إضافة تعريف واضح لحقل `الباقه`.
- التحقق من عدم تكرار `Code` أو أرقام الهوية.

---

## 8. Instructions for AI Analysis

يمكن للذكاء الاصطناعي تنفيذ التحليلات التالية:

- حساب عدد الموظفين حسب المشروع.
- حساب عدد الموظفين حسب الإدارة أو القسم.
- توزيع الموظفين حسب الجنسية أو الجنس.
- حساب متوسط الأعمار.
- حساب مدة الخدمة من تاريخ التعيين.
- تحديد العقود أو الإقامات أو الجوازات التي ستنتهي قريباً.
- تحديد السجلات غير المكتملة.
- اكتشاف الأكواد المكررة.
- اكتشاف التواريخ غير الصالحة.
- اكتشاف أرقام الهواتف أو رسائل البريد الإلكتروني غير المكتملة.

عند حساب عدد الموظفين:

- استخدم السجلات التي تحتوي على `Code` و`Name`.
- لا تحسب الصفوف التي تحتوي على `Code` فقط كسجلات موظفين مكتملة.
- استخدم `Code` كمعرف الموظف الأساسي.
- لا تستخدم الاسم وحده كمفتاح، لأن طريقة كتابة الاسم قد تختلف.

---

## 9. Expiry Classification

عند تحليل تواريخ انتهاء الإقامة أو الجواز أو العقد، استخدم التصنيف التالي:

- `Expired`: تاريخ الانتهاء قبل تاريخ اليوم.
- `Critical`: تاريخ الانتهاء خلال 30 يوماً.
- `Due Soon`: تاريخ الانتهاء خلال 31 إلى 90 يوماً.
- `Valid`: تاريخ الانتهاء بعد أكثر من 90 يوماً.
- `Missing`: لا يوجد تاريخ صالح.

يجب ذكر تاريخ تنفيذ التقرير لأن حالة المستندات تتغير بمرور الوقت.

---

## 10. Example Questions

أمثلة على الأسئلة التي يمكن توجيهها للذكاء الاصطناعي:

- كم عدد الموظفين في كل مشروع؟
- ما توزيع الموظفين حسب الإدارة؟
- ما المستندات التي ستنتهي خلال 90 يوماً؟
- من الموظفون الذين لا يوجد لهم بريد إلكتروني؟
- ما السجلات التي تحتوي على بيانات ناقصة؟
- هل توجد أكواد موظفين مكررة؟
- ما متوسط عمر الموظفين حسب المشروع؟
- ما عدد الموظفين المصريين والعمانيين؟
- ما الموظفون المصنفون كـ `Engineering`؟
- ما العقود المنتهية أو التي قاربت على الانتهاء؟

---

## 11. Output Rules

عند إعداد تقرير:

- ابدأ بملخص تنفيذي قصير.
- اذكر عدد السجلات المستخدمة في التحليل.
- اذكر تاريخ تنفيذ التحليل.
- وضح كيفية التعامل مع القيم الفارغة والرمز `-`.
- استخدم الجداول المجمعة بدلاً من عرض البيانات الشخصية.
- أخفِ البيانات الحساسة عند الحاجة.
- وضح أي افتراضات أو حقول غير معروفة.
- لا تخترع بيانات غير موجودة في الشيت.